# Default ProGuard/R8 rules. Add app-specific keep rules here as needed.
# Firebase Firestore data classes (added in M1) should be kept from obfuscation,
# e.g. with @Keep or explicit -keep rules, so document IDs/fields map correctly.
