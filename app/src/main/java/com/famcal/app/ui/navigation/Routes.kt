package com.famcal.app.ui.navigation

/**
 * Navigation destinations for the app. Kept as simple string routes for now;
 * these will grow as screens are added in later milestones.
 */
object Routes {
    const val AUTH = "auth"
    const val FAMILY_SETUP = "family_setup"
    const val CALENDAR = "calendar"
    const val EVENT_EDITOR = "event_editor"
    const val MEMBERS = "members"
    const val SETTINGS = "settings"
}
